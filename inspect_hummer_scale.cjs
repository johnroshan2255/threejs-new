
const fs = require('fs');
const THREE = require('three');
const { GLTFLoader } = require('three/examples/jsm/loaders/GLTFLoader.js');
// Need DOM/XHR polyfill for GLTFLoader in Node if we actually parse it.
// Instead, let's just create a basic hummerCarVisual.ts first.

